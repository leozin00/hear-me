<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#eff2f0</item>
        <item name="colorPrimaryDark">#C4C8C9</item>
        <item name="colorAccent">#ff0000</item>
        <item name="title">@string/app_name</item>
        <item name="titleTextColor">#000</item>
        <item name="android:actionOverflowButtonStyle">@style/icon_config</item>


    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="icon_config" parent="android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/icon_config</item>

        <item name="android:maxWidth">50dp</item>
        <item name="android:minWidth">35dp</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />




</resources>
